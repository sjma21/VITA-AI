# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Postgres (matches docker-compose.yml defaults)
DATABASE_URL="postgresql://vita:vita@localhost:5432/vita?schema=public"

# LLMs (used from Phase 1/2 onward)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# GitHub read-only token (Phase 1 ingest + tools)
GITHUB_TOKEN=
GITHUB_USERNAME=

# Optional admin gate (Phase 4)
ADMIN_PASSWORD=
